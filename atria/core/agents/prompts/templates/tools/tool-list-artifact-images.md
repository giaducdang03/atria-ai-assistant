List artifact images stored in the conversation or project scope.

Returns a list of artifact metadata including IDs, filenames, types, sizes, and creation timestamps. Use this to discover available artifacts before reading them with `read_artifact_image`.

**Scope Options:**
- `conversation`: List artifacts scoped to the current conversation only
- `project`: List artifacts scoped to the entire project
